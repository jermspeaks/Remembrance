<div class="new-post">
  <h1>Create a new post</h1>

  <div class="new_post_form">
    <%= form_for([@memorial, @memorial.posts.build]) do |f| %>
      <%= f.label :text %><br>
      <%= f.text_area :text %>

      <%= f.submit %>
    <% end %>
  </div>
</div>
